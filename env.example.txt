# Slack Bot Configuration
# Copy this file to .env and fill in your actual values

# Your Slack Bot User OAuth Token (starts with xoxb-)
# Get this from: https://api.slack.com/apps -> Your App -> OAuth & Permissions
SLACK_BOT_TOKEN= YOUR SLACK APP TOKEN

# Channel name to retrieve messages from (e.g., "general", "random")
# Or you can use channel ID instead by modifying the script
SLACK_CHANNEL_NAME=circle-ai

# Maximum number of messages to retrieve (default: 100)
SLACK_MESSAGE_LIMIT=100

# Only get messages from the last N minutes (leave empty or remove to get all messages)
# Example: 30 = only messages from the last 30 minutes
SLACK_MINUTES_AGO=30

# LLM Configuration
# LLM Provider: "openai" or "groq" (default: openai)
LLM_PROVIDER=openai

# Model name (optional, uses provider defaults if not set)
# OpenAI defaults: gpt-4o-mini
# Groq defaults: llama-3.1-70b-versatile
# Other Groq models: llama-3.1-8b-instant, mixtral-8x7b-32768, gemma-7b-it
LLM_MODEL=

# OpenAI API Key (required if LLM_PROVIDER=openai)
OPENAI_API_KEY=your-openai-api-key-here

# Groq API Key (required if LLM_PROVIDER=groq)
# Get your key from: https://console.groq.com/keys
GROQ_API_KEY=your-groq-api-key-here

# API Configuration (for integration script)
API_URL=http://localhost:8000/extract-todos

# Logging Configuration
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# Scheduler Configuration
# Enable/disable automatic todo extraction (default: true)
SCHEDULER_ENABLED=true

# Interval in minutes for automatic todo extraction (default: 30)
SCHEDULER_INTERVAL_MINUTES=30

# Post todos back to Slack channel (default: true)
# Set to false to disable posting todos to Slack
POST_TODOS_TO_SLACK=true

